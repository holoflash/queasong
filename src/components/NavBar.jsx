import '../styles/navbar.scss'
import { SpotifyProfile } from './SpotifyProfile';

const NavBar = () => {
    return (
        <div id="navbar">
            <SpotifyProfile />
        </div>
    );
};

export default NavBar;
